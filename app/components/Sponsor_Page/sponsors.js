import React from 'react';

const Sponsor = React.createClass({
  render: function() {
    return (
      <div className="sponsor-page">
        <h1>Sponsor Page</h1>
        <p>
         All teams competiting in the UW EcoCAR3 competition will be provided a Chevy Camaro by General Motors. 
         Teams are expected to remove the stock powertrain and replace it with our own more fuel efficient powertrain.
        </p>
      </div>
    );
  }
});

export default Sponsor;
